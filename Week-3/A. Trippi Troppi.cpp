#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);


    int n;
    cin >> n;

    while(n--){
        string a, b, c;

        cin >> a >> b >> c;

       cout << a[0] << b[0] << c[0]<<endl;
    }

    

    return 0;
}