#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<int> arr;

    arr.push_back(1000);
    arr.push_back(10);
    arr.push_back(100);
    arr.push_back(1000);
    arr.push_back(100);
    arr.push_back(100);
    arr.push_back(100);


    // arr.pop_back();

    // cout << arr.front();
    cout << arr.size();




    
    

    return 0;
}